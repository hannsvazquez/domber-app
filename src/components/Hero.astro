---
const headlines = [
  'Reparación de lavadoras',
  'Paseo de mascotas',
  'Servicio de limpieza',
  'Cambio de aceite',
  'Barberia en casa'
];
---

<div class="flex flex-col items-center justify-center min-h-[80vh] py-8">
  <!-- Logo Icon -->
  <img src="/icon-logo.png" alt="Domber Icon Logo" class="w-24 h-24 mb-6" />
  <!-- Beta Button -->
  <button class="mb-8 px-6 py-2 border-2 border-primary rounded-full font-sans text-primary text-lg bg-transparent btn-hero">
    versión beta
  </button>

  <!-- Subtitle -->
  <h2 class="font-sans text-primary text-center mb-2" style="font-size:1.75rem;">Encuentra el mejor servicio para tu hogar en minutos</h2>

  <!-- Animated Headline -->
  <h1 class="font-sans text-primary text-center font-bold mb-6 relative" style="font-size:3.4375rem;">
    <span id="headline-highlight" class="bg-secondary px-2 rounded transition-colors duration-300 inline-block">
      Reparación de lavadoras
    </span>
  </h1>


  <!-- Supporting Image -->
  <img src="/supporting-iphone-mockup.png" alt="App Mockup" class="w-64 max-w-full mt-2" />
</div>

<script>
  // Simple text cycling animation for the headline
  const headlines = [
    'Reparación de lavadoras',
    'Paseo de mascotas',
    'Servicio de limpieza',
    'Cambio de aceite',
    'Barberia en casa'
  ];
  let idx = 0;
  setInterval(() => {
    idx = (idx + 1) % headlines.length;
    document.getElementById('headline-highlight').textContent = headlines[idx];
  }, 2300);
</script>
